// #MAP "VIA" "storage": next storage declaration becomes VIA.
// Following assign statements only wire the node; they are not part of the #MAP.
module map_via(
    finput in,
    foutput out
);
    #MAP "VIA" "storage"
    storage via0;
    assign via0 = in;
    assign out = via0;
endmodule
